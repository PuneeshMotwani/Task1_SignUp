package Page;

import org.apache.commons.logging.Log;
import org.openqa.selenium.Keys;
import org.openqa.selenium.Point;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.interactions.Actions;

import Utility.LogUtil;

public class CommonPageActions extends PageLibrary {
	public static Log logger = LogUtil.getLog(CommonPageActions.class);
	
	
	/*
	 * This class will contain the methods for common functions within different pages of applicaton. e.g. Login, 
	 * log-out, headers, footers, etc
	 */

	
	
}































